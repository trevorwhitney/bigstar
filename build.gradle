subprojects {
    defaultTasks "clean", "build"

    buildscript {
        repositories {
            jcenter()
        }
    }

    repositories {
        jcenter()
    }

    ext {
        spring_version = "4.1.8.RELEASE"
        kotlin_version = '1.0.4'
    }
}
